language: node_js
node_js:
  - '10'
cache:
  directories:
    - ~/.npm
    - ~/.cache
before_install:
  - yarn -g http-server
install:
  - npm ci
script:
  - yarn test
  - yarn cypress:ci
after_script: "cat ./coverage/lcov.info | coveralls"
